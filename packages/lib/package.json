{
  "name": "@gh-actions/lib",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "node scripts/embed-version.ts",
    "build": "pnpm generate && tsc -b tsconfig.build.json",
    "test": "pnpm generate && vitest run",
    "typecheck": "pnpm generate && tsc -b"
  },
  "devDependencies": {
    "@types/lodash-es": "^4.17.12",
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2",
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "lodash-es": "^4.18.1"
  }
}
